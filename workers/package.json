{
  "name": "sbconfig-workers",
  "version": "1.0.0",
  "description": "Supabase Configuration Generator - Cloudflare Workers",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --config wrangler.staging.toml",
    "deploy:production": "wrangler deploy --config wrangler.toml",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No linting configured yet\" && exit 0"
  },
  "keywords": [
    "supabase",
    "configuration",
    "cloudflare",
    "workers",
    "serverless"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
